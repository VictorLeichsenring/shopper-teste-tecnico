const DriverController = require('./driver.controllers');
const GoogleController = require('./google.controllers');
const RideController =require('./ride.controller');

module.exports = {
  DriverController,
  GoogleController,
  RideController,
}